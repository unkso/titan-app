/**
 * Add the default export for new extension to this array. The extension should
 * export the following structure:
 *
 * {
 *    config: {...},
 *    name: "...",
 *    reducer: createStateReducer([...]),
 *    routes: [...]
 * }
 */
export default []
